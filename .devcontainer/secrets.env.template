# Copy this file to secrets.env and fill in your actual Azure credentials
# The secrets.env file will be ignored by git for security

# Azure AI Foundry Project Endpoint
# Format: https://<your-project-name>.services.ai.azure.com/api/projects/<project-id>
# You can find this in the Azure AI Foundry portal under your project's Overview section
AZURE_AI_PROJECT_ENDPOINT=your_azure_ai_project_endpoint_here

# Azure OpenAI Endpoint (Required for Agent Framework)
# Format: https://<your-resource-name>.openai.azure.com
# You can find this in Azure Portal > Azure OpenAI resource > Keys and Endpoint
AZURE_OPENAI_ENDPOINT=your_azure_openai_endpoint_here

# Azure Authentication (Optional - only needed if NOT using 'az login')
# For local development, it's recommended to use 'az login' instead of these credentials
# For production/CI-CD, you can use these service principal credentials

# AZURE_TENANT_ID=your_azure_tenant_id_here
# AZURE_CLIENT_ID=your_azure_client_id_here
# AZURE_CLIENT_SECRET=your_azure_client_secret_here

# Note: The DefaultAzureCredential will automatically try multiple authentication methods:
# 1. Environment variables (AZURE_TENANT_ID, AZURE_CLIENT_ID, AZURE_CLIENT_SECRET)
# 2. Azure CLI credentials (from 'az login')
# 3. Managed Identity (when running in Azure)
# 4. Visual Studio Code credentials
# 5. And more...
